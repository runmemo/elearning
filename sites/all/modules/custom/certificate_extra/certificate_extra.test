<?php

/**
 * @file
 * 
 * Test cases for the certificate_extra module.
 * Dummy test checks module installed correctly.
 */

/**
 * Base class for certificate_extra tests.
 */
class CertificateExtraBaseCase extends DrupalWebTestCase {

  function setUp() {
    parent::setUp('certificate_extra');
  }

}

/**
 * Tests checks install of certificate_extra module.
 */
class CertificateExtraCase extends CertificateExtraBaseCase {

  public static function getInfo() {
    return array(
      'name' => 'Certificate Extra Test',
      'description' => 'Test to check certificate_extra module setup',
      'group' => 'Course',
    );
  }

  function setUp() {
    parent::setUp();
  }

  function testSetupSuccess() {
    $this->assertTrue(TRUE, 'Certificate Extra module setup complete.');
  }
}

